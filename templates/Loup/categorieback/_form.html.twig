{{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
<table>
    
    <tr>
        <td>
            {{ form_label(form.nomCat,"name : ") }}
        </td>
        <td>
            {{ form_widget(form.nomCat) }}
        </td>
        <td>
            {{ form_errors(form.nomCat) }}
        </td>

    </tr>
    
</table>

    <button class="btn">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}