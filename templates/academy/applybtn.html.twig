{# <a href="{{ path('app_academy_apply', { 'id': academy.id }) }}" class="btn btn-primary">Apply</a> #}
<form method="post" action="{{ path('app_academy_apply', {'id': academy.id}) }}">
    <button class="ap" ><img src="{{ asset ('rami/public/icons/apply.png') }}" alt="Edit" width="25" height="25"><span> Apply</span></button>
    <style>
    .ap {
        background-color: #FFFFFF; /* Green */
        border: none;
        color: black;
        padding: 2px 6px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 4px 2px;
        cursor: pointer;
        border-radius: 10px;
        box-shadow: 0px 4px 5px rgba(0, 0, 0, 0.2);
        }

        .ap:hover {
        background-color: #6A5ACD; /* Darker green */
        }

        </style>
    </form>