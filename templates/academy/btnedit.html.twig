<form method="post" action="{{ path('app_academy_edit', {'id': academy.id}) }}">
    <button class="ed" ><img src="{{ asset ('rami/public/icons/edit.png') }}" alt="Edit" width="25" height="25"><span> Edit</span></button>
    <style>
        .ed {
        background-color: #FFFFFF; /* Green */
        border: none;
        color: black;
        padding: 2px 6px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 4px 2px;
        cursor: pointer;
        border-radius: 10px;
        box-shadow: 0px 4px 5px rgba(0, 0, 0, 0.2);
        }

        .ed:hover {
        background-color: #1E90FF; /* Darker green */
        }

        </style>
    </form>