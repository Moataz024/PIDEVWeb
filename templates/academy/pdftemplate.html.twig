<!DOCTYPE html>
<html>
<head>
	<style>
		h1 {
			color: #c0392b;
			font-size: 32px;
			margin-bottom: 20px;
		}
		p {
			font-size: 18px;
			margin-bottom: 10px;
		}
	</style>
</head>
<body>
	<h1>Application Receipt</h1>

	<p><strong>Date:</strong> {{ "now"|date("Y-m-d H:i:s") }}</p>
	<p><strong>Name:</strong> {{ application.getAppname() }}</p>
	<p><strong>Age:</strong> {{ application.getAppage() }}</p>
	<p><strong>Academy:</strong> {{ application.getAcademy().getName() }}</p>
	<p><strong>Username:</strong> {{ application.getUser().getNomutilisateur() }}</p>
	<p><strong>Email:</strong> {{ application.getUser().getEmail() }}</p>
	<p><strong>Telephone:</strong> {{ application.getUser().getPhone() }}</p>
</body>
</html>
