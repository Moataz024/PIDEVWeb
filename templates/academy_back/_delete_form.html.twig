<form method="post" action="{{ path('app_academy_back_delete', {'id': academy_back.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ academy_back.id) }}">
    <button class="del"><img src="{{ asset ('icons/trashbg.png') }}" alt="Delete" width="25" height="25"> Delete</button>
    <style>
    .del {
    background-color: #FF0000; /* Green */
    border: none;
    color: white;
    padding: 2px 6px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 4px 2px;
    cursor: pointer;
    border-radius: 10px;
    box-shadow: 0px 4px 5px rgba(0, 0, 0, 0.2);
    }

    .del:hover {
    background-color: #8B0000; /* Darker green */
    }

    </style>

</form>
    
