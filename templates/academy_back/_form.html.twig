{# {{ form_start(form) }}
    {{ form_widget(form) }} #}
    <tr>
        <td>
            <button class="btn">{{ button_label|default('Save') }}<img src="{{ asset ('rami/public/icons/save.png') }}" width="45" height="45"></button>
            <style>
            .btn {
            background-color: #4CAF50; /* Green */
            border: none;
            color: white;
            padding: 2px 6px;
            text-align: center;
            
            /* text-decoration: overline blue; */
            /* text-decoration: none; */
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
            border-radius: 10px;
            box-shadow: 0px 4px 5px rgba(0, 0, 0, 0.2);
            }

            .btn:hover {
            background-color: #3e8e41; /* Darker green */
            text-decoration: underline;
            }

            </style>
        </td>
    </tr>
    
{# {{ form_end(form) }} #}
