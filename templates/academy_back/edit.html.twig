{% extends "back/base.html.twig" %}

{% block body %}
    <h1>Edit academy</h1>

    {{ include('academy_back/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_academy_back_index') }}">back to list</a>

    {{ include('academy_back/_delete_form.html.twig') }}
{% endblock %}
