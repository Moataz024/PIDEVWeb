{% extends "back/base.html.twig" %}

{% block body %}
    <h1>Academy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ academy_back.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ academy_back.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ academy_back.category }}</td>
            </tr>
            {# <tr>
                <th>Coach</th>
                <td>{{ academy_back.Coach }}</td>
            </tr> #}
        </tbody>
    </table>

    <a href="{{ path('app_academy_back_index') }}">back to list</a>

    <a href="{{ path('app_academy_back_edit', {'id': academy_back.id}) }}">edit</a>

    {{ include('academy_back/_delete_form.html.twig') }}
{% endblock %}
