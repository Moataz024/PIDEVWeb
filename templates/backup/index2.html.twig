{% extends "academy/base.html.twig" %}

{% block title %}Academy{% endblock %}



{% block header %}

<div class="site-wrap">

    <div class="site-mobile-menu site-navbar-target">
        <div class="site-mobile-menu-header">
            <div class="site-mobile-menu-close">
                <span class="icon-close2 js-menu-toggle"></span>
            </div>
        </div>
        <div class="site-mobile-menu-body"></div>
    </div>

    <header class="site-navbar py-4" role="banner">
        <div class="container">
            <div class="d-flex align-items-center">
                <div class="logo">
                    <a href="{{ path('app_template') }}">
                        <img src="{{ asset ('front/images/sportify.png') }}" alt="Logo" width="140" height="30">
                    </a>
                </div>
                <div class="ml-auto">
                    <nav class="site-navigation position-relative text-right" role="navigation">
                        <ul class="site-menu main-menu js-clone-nav mr-auto d-none d-lg-block">
                            <li><a href="{{ path('app_template') }}" class="nav-link">Home</a></li>
                            <li><a href="{{ path('app_event_index') }}" class="nav-link">Events</a></li>
                            <li><a href="#" class="nav-link">Products</a></li>
                            <li><a href="#" class="nav-link">Equipments</a></li>
                            <li class="active"><a href="{{ path('app_academy_index') }}" class="nav-link">Academy</a></li>
                            <li><a href="{{ path('app_coach_index') }}" class="nav-link">Coach</a></li>
                            {# <li><a href="{{ asset ('front/contact.html') }}" class="nav-link">Contact</a></li> #}
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <img src="{{ asset('assets/img/user.png') }}" alt="User" width="20" height="20" class="rounded-circle"> <b>{{ app.user.nomutilisateur }} </b>
                                </a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                                    <a class="dropdown-item" href="{{ path('app_profile',{'id': app.user.id}) }}">My profile</a>
                                    <a class="dropdown-item" href="{{ path('change_password_front') }}">Change password</a>
                                    <a class="dropdown-item" href="{{ path('app_logout') }}">Log Out</a>
                                </div>
                            </li>
                        </ul>
                    </nav>

                    <a href="#" class="d-inline-block d-lg-none site-menu-toggle js-menu-toggle text-black float-right text-white"><span
                                class="icon-menu h3 text-white"></span></a>
                </div>
            </div>
        </div>


    </header>
{% endblock %}

{% block body %}

    <div class="hero overlay" style="background-image: url({{ asset('academy/3.png') }});background-color: crimson;;">{#background-size: 400px 200px;#}
    <div class="container">
        <div class="row align-items-center">
        <div class="col-lg-9 mx-auto text-center">
            <h1 class="text-white">Academy</h1>
        </div>
        </div>
    </div>
    </div>
{% endblock %}


{% block content %}
<div class="container">
            <div class="row align-items-center" >
    <h1>Academies list</h1>
    {# <h1 style="font-size:30px; color:white; margin:10px;">Academies list</h1> #}
    <html>
    <head>
    <style>
    .table {
        border-collapse: collapse;
        margin: 25px auto;
        font-size: 1em;
        font-family: sans-serif;
        min-width: 400px;
        /* box-shadow: 0 0 20px rgba(0, 0, 0, 0.15);
        border: none; */
    }
    .table thead tr {
    background-color: #3CB371;
    color: #ffffff;
    text-align: left;
    }
    .table th,
    .table td {
        border: none;
        text-align: left;
        padding: 1px 2px;
    }
    /* .table tbody tr {
        border-bottom: 1px solid #dddddd;
    } */

    /* .table tbody tr:nth-of-type(even) {
        background-color: #f3f3f3;
    } */

    /* .table tbody tr:last-of-type {
        border-bottom: 2px solid #009879;
    } */
    /* table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }*/


    /* td, th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
    }  */

    </style>
    </head>
    <body>

    {# <table class="table table-responsive     table-bordred table-striped"> #}
    {# {% if is_granted('ROLE_OWNER') or is_granted('ROLE_ADMIN') %} #}
        <table class="table table-striped">
            <thead>
                <tr>
                    {# <th>Id</th> #}
                    <th>Name</th>
                    <th>Category</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for academy in academies %}
                <tr>
                    {# <td>{{ academy.id }}</td> #}
                    <td>{{ academy.name }}</td>
                    <td>{{ academy.category }}</td>
                    <td>
                        <table>
                            <tr>
                                <td>{{ include('academy/btnshow.html.twig') }}</td>
                                
                                    {# {% if app.user.id == event.organisateur.id %} #}
                                <td>{{ include('academy/btnedit.html.twig') }}</td>
                                
                            </tr>
                        </table>
                        {# <a href="{{ path('app_academy_show', {'id': academy.id}) }}"><img src="{{ asset ('icons/show.png') }}" alt="Show" width="25" height="25"></a>
                        <a href="{{ path('app_academy_edit', {'id': academy.id}) }}"><img src="{{ asset ('icons/edit.png') }}" alt="Edit" width="25" height="25"></a> #}
                        {# {{ include('academy/btnshow.html.twig') }}
                        {{ include('academy/btnedit.html.twig') }} #}
                        {# {{ include('academy/_delete_form.html.twig') }} #}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        </body>
        </html>
        

        <a href="{{ path('app_academy_new') }}">Add<img src="{{ asset ('icons/add.png') }}" width="20" height="20"></a>
    
{% endblock %}

