{% extends 'base.html.twig' %}

{% block title %}CardItem{% endblock %}

{% block body %}
    <h1>CardItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ card_item.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ card_item.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_card_item_index') }}">back to list</a>

    <a href="{{ path('app_card_item_edit', {'id': card_item.id}) }}">edit</a>

    {{ include('card_item/_delete_form.html.twig') }}
{% endblock %}
