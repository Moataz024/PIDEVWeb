{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn"><span class="material-icons">
        update
        </span> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
