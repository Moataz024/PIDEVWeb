{# {% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block content %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ commande.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ commande.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ commande.email }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ commande.tel }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ commande.city }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ commande.adresse }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_commande_index') }}">back to list</a>

    <a href="{{ path('app_commande_edit', {'id': commande.id}) }}">edit</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %} #}


{% extends 'back/base.html.twig' %}

{% block title %}Card{% endblock %}

{% block body %}


<table>
    <thead>
        <tr>
            <th>Product</th>
            <th>Quantity</th>
            <th>Price</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% set total = 0 %} 
        {% for item in commande.getHistoriques() %}
            <tr>
                <td>{{ item.libelle}}</td>
                <td>{{ item.quantity }}</td>
                <td>{{ item.prix * item.quantity }}</td>
                 <td>
                    <td>
                        {{ include('historique/_delete_form.html.twig') }}
                     </td>

                 </td>

            </tr>
             {% set total = total + (item.prix * item.quantity) %}
        {% endfor %}

    </tbody>
</table>
<p>Total: {{ total }}</p>
 <a href="{{ path('app_commande_index',{}) }}">back to list</a>
{# <a href="{{ path('app_checkout',{'id':cart.id,'id_user':app.user.id}) }}">Commander</a> #}
{% endblock %}
{% block score %}
{% endblock %}
{% block latest %}
{% endblock %}
{% block nextmatch %}
{% endblock %}
{% block sitesection %}
{% endblock %}
{% block blog %}
{% endblock %}
{% block footer %}
{{ parent() }}
{% endblock %}