<form method="post" action="{{ path('app_rami_delete', {'id': rami.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rami.id) }}">
    <button class="btn">Delete</button>
</form>
