{% extends "front/base.html.twig" %}

{% block body %}
    <h1>Edit Rami</h1>

    {{ include('rami/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_rami_index') }}">back to list</a>

    {{ include('rami/_delete_form.html.twig') }}
{% endblock %}
