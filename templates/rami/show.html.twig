{% extends "front/base.html.twig" %}

{% block body %}
    <h1>Rami</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rami.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rami.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ rami.age }}</td>
            </tr>
            <tr>
                <th>Field</th>
                <td>{{ rami.field }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rami_index') }}">back to list</a>

    <a href="{{ path('app_rami_edit', {'id': rami.id}) }}">edit</a>

    {{ include('rami/_delete_form.html.twig') }}
{% endblock %}
