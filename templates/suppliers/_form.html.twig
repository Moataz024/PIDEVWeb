
<div class="col-md-6 grid-margin stretch-card">
    <div class="card">
      <div class="card-body">
        {{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
    <button class="btn"> <span class="material-icons">
      update
      </span> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>
</div>
</div>