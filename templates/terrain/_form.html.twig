{{ form_start(form) }}

    {{ form_widget(form) }}
    <div style="padding-left: 20px">
    <button class="btn-outline-primary btn-lg" >{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
